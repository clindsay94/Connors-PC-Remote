<UserControl
    x:Class="CPCRemote.UI.Controls.AnimatedRadialGauge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    SizeChanged="UserControl_SizeChanged">

    <Grid>
        <!-- Glow Ring (Backlight) — uses category color -->
        <Ellipse x:Name="GlowRing"
                 Stroke="{ThemeResource NeonGlowBrush}"
                 StrokeThickness="10"
                 Width="100" Height="100"
                 Opacity="0.12"/>

        <!-- Background Ring -->
        <Ellipse x:Name="TrackRing"
                 Stroke="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                 StrokeThickness="7"
                 Width="100" Height="100"/>

        <!-- Progress Ring (Arc) — colored by category -->
        <Path x:Name="ProgressPath"
              Stroke="{ThemeResource NeonGlowBrush}"
              StrokeThickness="7"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              Width="100" Height="100"/>

        <!-- Content: Value + Unit in center -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="ValueText" 
                       Text="0" 
                       Style="{StaticResource TitleLargeTextBlockStyle}" 
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource VibrantMesh}"/>
            <TextBlock x:Name="UnitText" 
                       Text="%" 
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       HorizontalAlignment="Center"
                       Opacity="0.7"/>
        </StackPanel>
    </Grid>
</UserControl>
